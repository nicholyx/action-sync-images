# 安全策略

## 支持范围

本项目由个人业余维护，只为**最新一次发布**的版本提供安全修复。使用旧版本时请先升级再反馈问题。

| 版本 | 是否支持安全更新 |
| --- | --- |
| 最新 release | ✅ |
| 更早的 release | ❌ |
| `main` 分支 | ✅（开发中） |

## 报告安全漏洞

**请不要通过公开的 Issue 报告安全漏洞。**

请使用 GitHub 的私有漏洞报告渠道：

👉 [报告安全漏洞](https://github.com/nicholyx/action-sync-images/security/advisories/new)

如果该入口不可用，也可以直接发邮件给维护者（见 GitHub 个人主页），标题注明 `[SECURITY]`。

### 请在报告中包含

- 受影响的文件、工作流名称或提交哈希
- 复现步骤或最小复现样例
- 影响评估：攻击者能做什么，需要什么前置条件
- 如果你有修复思路，欢迎一并附上（但请不要直接提公开 PR）

### 我们的响应承诺

| 阶段 | 目标时间 |
| --- | --- |
| 确认收到 | 7 天内 |
| 初步评估与定级 | 14 天内 |
| 修复或缓解措施 | 视严重程度，尽快 |
| 公开披露 | 修复发布后，与你协商披露时机 |

## 本项目的威胁模型

理解本项目的攻击面，有助于你判断某个问题算不算安全漏洞。

### 这是一个「有凭证的自动化仓库」

本仓库的 GitHub Actions 中配置了指向**真实镜像仓库的推送凭证**：

- `DOCKER_USERNAME` / `DOCKER_PASSWORD` —— 阿里云容器镜像服务的账号密码
- `HARBOR_REGISTRY` / `HARBOR_USERNAME` / `HARBOR_PASSWORD` —— 自建 Harbor 的地址与凭证

任何能够修改 `.github/workflows/` 下文件的人，都可以在其工作流中读取这些 Secrets，
进而向你的镜像仓库推送任意内容，或删除已同步的镜像。**这是本项目最核心的风险。**

### 已采取的防护措施

- 工作流**仅通过 `workflow_dispatch` 手动触发**，不接受来自 Issue、PR 评论等外部输入的直接触发
- 仓库持有者之外的人员无法修改 `main` 分支（分支保护规则）
- 所有 `workflow_dispatch` 输入均通过 `env:` 中转后再引用，避免表达式注入
- CI 中包含 `actionlint`，会对工作流做静态检查
- [CODEOWNERS](.github/CODEOWNERS) 要求 `.github/workflows/` 的改动必须由维护者 review

### 明确不在威胁模型内的情况

以下情况**不算**本项目的安全漏洞，请不要作为漏洞提交：

- 你自己在 fork 中配置了弱密码、或把凭证写进了公开文件
- 你手动修改工作流后自己泄露了凭证
- 上游镜像仓库（`registry.k8s.io`、`gcr.io` 等）自身的可用性或内容问题
- 同步下来的镜像本身含有恶意内容 —— 本项目只做搬运，不校验镜像内容

## 如果你怀疑凭证已经泄露

1. 立即到阿里云 / Harbor 控制台**轮换密码**
2. 更新仓库的 Secrets
3. 检查镜像仓库的推送日志，确认是否有异常推送
4. 检查 Actions 运行记录，确认是否有非你发起的 `workflow_dispatch`
