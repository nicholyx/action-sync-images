# 更新日志

本文件记录本项目的所有重要变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [Unreleased]
### 修复

- **`history.sh --check` 的下载模式取不到检查报告**：取数窗口用的是「最近 20 次
  运行」且不按工作流过滤，而体检工作流只有手动触发——窗口几乎总被 CI 等高频
  运行挤占，结果是「一份报告都捞不到」（这不是数据不存在，是取数范围选错了；
  同步趋势的下载模式同样受影响，只是同步跑得更勤、暴露得晚）。现在下载模式
  支持 `--workflow` 过滤，`--check` 默认只看体检工作流（`Check-Registry`）的
  运行，相关提示文案也按模式区分
### 新增
- **趋势结果落盘并上 Actions 页面**（`history.sh --report-dir` +
  `History-Trend` 工作流）：趋势查询此前只有装了 CLI 的本地入口，结果只进
  stdout——不能归档，也不能给别的系统消费。现在 `--report-dir` 把趋势落盘为
  `.md`（与 stdout 同源）与 `.json`（`generated_at` / `query` / `summary` /
  `rows`，机器可读）；`History-Trend` 手动工作流在 Actions 页面就能跑趋势，
  结果进 Step Summary 并上传 Artifact，红绿与体检同一套（绿 = 窗口内没有
  需要处理的记录）。聚合口径单一来源：渲染与落盘共用同一组聚合函数

## [1.9.0] - 2026-09-13

本轮把审计数据用起来：锁文件时效校验有了云端入口，检查报告能聚合趋势——「哪个镜像一直落后 / 一直在漂移」从单次报告里的猜测，变成窗口内可回答的问题。

- **检查报告的趋势查询**（`history.sh --check audit` / `--check lock-audit`）：
  把历次检查报告摊在一起，回答「哪个镜像一直落后 / 一直缺失」「哪个锁条目
  一直在漂移」——单次报告只讲「这一次」，而漂移和落后恰恰要看「是不是一直」。
  数据复用 v1.8.0 起落盘的检查报告（体检工作流的 `check-report` Artifact），
  不引入新的存储。分组键是「源镜像 + 目标」（审计状态绑定目标仓库）；
  退出码 2 只由确定异常的历史触发，「无法判定」不触发（查不成不等于出问题，
  但在表格中独立可见）。`check-updates` 不支持趋势：未收录 tag 的增减没有
  趋势价值，显式传入得到参数错误
- **体检工作流支持锁文件校验**（`Check-Registry` 的 `mode=lock`）：锁文件时效
  校验（`--audit-lock`，v1.8.0）此前只有本地入口，现在在 Actions 页面就能跑——
  传入锁文件路径即可，报告与其他检查一样落盘上传（`check-report` Artifact），
  为「哪个镜像一直在漂移」的趋势积累云端数据。工作流新增 `lock_file` 输入
  （默认 `images.lock.resolved.txt`）；`--audit-lock` 脚本侧行为零改动
- **接入 Trellis 工程框架**：`.trellis/`（规范 / 任务 / 会话记忆）与 Claude Code
  平台层（`.claude/` 的 trellis-* skills 与 hooks）、根目录 `AGENTS.md`。规范按本仓库
  实际结构回填为四个目录（engine / workflows / maintenance / guides），全部条目
  来自真实代码与真实踩坑并给出出处；init 预置的 frontend / backend 模板层
  不适用已删除。维护循环流程规范同步沉淀进 `spec/maintenance/`，
  maintain-loop skill 补充「与 Trellis 的关系」一节。对使用者无感知，
  对贡献者的可见变化是：`.trellis/spec/` 是动手前应读的规范入口

## [1.8.0] - 2026-09-13

本轮回到数据本身：`--write-lock` 从「能复现」补全为「可校验」（`--audit-lock` 抓住上游悄悄覆盖 tag 的现场）；三种只读检查支持 `--report-dir`，跑完留下与同步同款的报告；macOS 本地同步不再静默失去超时保护。

### 新增

- **检查报告落盘**：`--audit` / `--check-updates` / `--audit-lock` 支持
  `--report-dir`，产出与同步报告同款的 `.md` + `.json`。此前两个只读检查
  跑完什么都不留——结果只存在于日志与 Step Summary 里，想让结果留档或给
  别的系统消费只能截图。JSON 顶层带 `generated_at` 与汇总计数，逐条记录
  带状态与备注；md 与 Step Summary 三方同源，不会各自漂移。
  `Check-Registry` 体检工作流同步上传报告 Artifact（30 天保留，与同步
  报告同一机制——这不是引入新的存储，Artifact 本来就是项目的既有约定）

- **锁文件时效性校验**（`--audit-lock <文件>`）：只读校验 `--write-lock` 生成的
  锁文件——锁文件里每个「镜像@digest」的上游，现在还是不是锁定的那份。
  `--write-lock` 只完成了「能复现」这半件事：上游重新构建并覆盖同名 tag 时
  锁文件不会有任何变化，而下一次增量同步会把新内容静默搬过去，等集群行为
  异常才发现。三种状态：一致 / 漂移 / 无法判定；上游 tag 已删除算漂移
  （明确发生的变更，不是「查不到」）。比对口径与 `--write-lock` 同源，
  「一致」意味着的正是「当时锁的就是这份」。未锁定 digest 的行与锁文件标注行
  出现在报告里但不参与判定。不需要目标地址

### 修复

- **macOS 本地同步不再静默失去超时保护**：`setup_timeout` 找不到 `timeout` /
  `gtimeout` 时（macOS 默认就没有），现在用系统自带的 perl `alarm` 兜底。
  此前所有 macOS 本地使用者开箱即处于无保护状态——一个大镜像或网络挂起会
  卡住整个批量任务，而这正是 `--timeout` 要防的事；警告里建议的 coreutils
  则要求使用者为项目装系统依赖。CI 的 ubuntu 永远走不到这个分支，
  因此冒烟测试直接测兜底机制本身

---

## [1.7.0] - 2026-09-12

本轮把 v1.6.0 做出的两条只读检查从「本地 CLI 的能力」变成「谁都能用的能力」：新增体检工作流让不装 CLI 的人也能在网页上跑检查，检查结果也能推送通知，并把八条设计记录补进架构文档。三项都建立在「检查是只读的」之上，没有触碰「同步必须显式触发」这条红线。

### 新增

- **体检工作流**（`Check-Registry`）：把 v1.6.0 的两条只读检查带到 Actions 页面。
  此前它们只能在本地命令行跑，而项目的主力使用者是通过网页操作的人——不装 CLI、
  也不克隆仓库，于是「检查」这类最该被用上的能力反而只有少数人能用得上。用一个
  `mode` 输入选 `audit`（清单 vs 目标仓库）或 `updates`（清单 vs 上游）；结果进
  Step Summary，配了 webhook 就推送到群里（默认只在有落后 / 缺失 / 无法判定时打扰）。
  体检发现差异时运行**标红**：绿 = 一切正常，红 = 需要看看——不是「检查失败」，
  而是「检查有结论」。`updates` 模式不需要目标地址，工作流也不会把它传给脚本。
  **工作流只有手动触发**：需要定时的使用者可以 fork 后自行加一行 `schedule`
- **检查结果也能推送通知**：`--audit` 与 `--check-updates` 支持 `--notify-webhook`，
  补上「无人值守体检」的最后一块——此前只有同步结果能通知，定期跑体检仍然得有人
  天天去运行页面看。`--notify-on failure` 在检查模式下的含义是「有需要关注的项」
  （落后 / 缺失 / 无法判定，或有仓库没查成），全绿时不打扰；通知只列需要处理的
  条目（最多 20 条），不会把整张表推过去把重点淹没。`--notify-after-failures`
  仅同步模式适用，检查模式下显式传入会告警

### 变更

- **架构文档补齐 v1.6.0 引入的只读检查设计记录**：新增八条「为什么」——检查与搬运
  为什么拆开、「无法判定」为什么必须单独成类（含判据为何刻意保守）、`--audit` 与
  `--strip-attestation` 为什么互斥且直接报错、上游版本检查为什么不做语义化判断
  （附两个被否掉的方案）、两条检查为什么互斥、退出码 `2` 为什么包含「没查完」、
  命名规则为什么挂在每个目标上，以及为什么不给体检加定时触发器。顺带修正「代码结构」
  里两处滞后（`scorecard.yml` 与 `history.sh` 都没列进去）

### 修复

- **配了 webhook 时，macOS 上的同步会以非零退出**：`for line in "${alert_lines[@]}"`
  在数组为空时，bash 3.2（macOS 自带）配合 `set -u` 会抛 unbound variable——
  同步明明成功了，脚本却报错退出，使用者会以为同步失败。触发条件是「全部成功 +
  配置了 webhook + 在 bash 3.2 上运行」，CI 用的 bash 5 不报此错，因此一直没暴露。
  改为先判长度再遍历（与 `collect_images` 里的既有做法一致）

## [1.6.0] - 2026-09-12

本轮把「该同步什么」从同步动作里拆了出来：`--audit` 只读回答「仓库与清单的差距」，`--check-updates` 只读回答「上游有没有新版本」；`--dest-keep-path` 解开了多目标共用一个命名规则的别扭。此外补齐供应链加固（pin SHA / zizmor / Scorecard）、英文版 README 与 SUPPORT.md，并把维护流程沉淀为两个项目级 skill。

### 新增

- **按目标选择命名规则**（`--dest-keep-path`）：`--dest` 一直用压平规则（`/` → `_`），
  因为阿里云 ACR 个人版不支持多级仓库路径；而自建 Harbor 支持多级路径，保留原路径
  也更符合直觉。此前多目标只能共用一套规则，「阿里云给国内集群 + Harbor 做内部归档」
  这个最典型的组合必然有一边别扭。现在每个目标可以各自选择，两者可任意混用、顺序无关。
  注意：源 registry 带端口时（`localhost:5000/foo`）端口那段仍会压成下划线——
  仓库路径不允许出现冒号
- **上游版本检查**（`--check-updates`）：拉取上游 tag 列表与清单对比，报告哪些
  版本还没收录——上游发新版时没有任何机制会通知你，这是定期同步流程里唯一还需要
  人肉盯着的环节。**只报告，不修改清单**（升到哪个版本涉及兼容性判断，是人的决定）；
  **不做语义化版本判断、不过滤预发布**（上游命名未必规整，`latest` / `1.27-alpine`
  / `v1.32.0-rc.1` 混在一起时语义化比较会给出错误结论），只用版本序粗排并原样展示。
  默认只列版本序最大的 5 条（`--updates-limit` 可调），但总数始终给出。不需要
  目标地址，同一源仓库的多个 tag 只查一次上游
- **清单审计**（`--audit`）：只读检查清单里每个镜像在目标仓库中的状态，**不推送
  任何东西**，回答「我的仓库跟上清单了吗」——这个问题此前只有真的跑一次同步才能
  回答，而同步是会真推送的。四种状态分开报告：最新 / 落后 / 缺失 / 无法判定。
  「无法判定」单独占一类是刻意的：把网络抖动显示成「落后」，会让人去排查一个
  并不存在的问题；源自身取不到时报的也是「无法判定」，而不是目标「缺失」。
  与 `--strip-attestation` 互斥（后者会重建索引，审计只会给出一排假的「落后」）。
  退出码 `2` 表示「未得出全部最新」，含「没查完」的情况，便于接进 CI 做定期体检
- **英文版 README**（`README.en.md`）：面向国际使用者的完整入口，覆盖全部特性、
  快速开始、参数速查与常见场景；两个 README 顶部互相链接切换。完整文档仍以
  中文为主，英文版已注明。顺带修正中文 README 滞后的路线图——私有源凭证、
  历史趋势、完整性校验、多目标单拉取、供应链加固等已完成项此前仍不在列
- **skill 沉淀**：`maintain-loop` 新增「退出码与进程模型」硬规则（包装函数
  吞退出码、命令替换的子 shell 传值、find 退出码语义等，全部来自真实踩坑）
  与「修改 YAML 工作流的工具选择」；`oss-bootstrap` 新增供应链加固清单
  （pin SHA / persist-credentials / zizmor / Scorecard）、SUPPORT.md 与
  多语言 README 的条目
- **SUPPORT.md**：获取帮助入口（文档 → Discussions Q&A → Bug → 安全报告的
  分流路径），这是 GitHub 社区标准文件之一，此前只有 README 里的零散指引

### 安全

- **供应链加固**（对标 OSSF Scorecard 检查项）：所有工作流引用的 Actions 从
  版本号 pin 到 commit SHA（注释保留版本，Dependabot 仍会提更新 PR）；
  所有 checkout 加 `persist-credentials: false`，GITHUB_TOKEN 不再残留在
  runner 上；Dependabot 引入 7 天冷却期
- **工作流安全扫描**：CI 新增 zizmor job（容器按版本 pin），专查 Actions
  的已知反模式；豁免项集中在 `.github/zizmor.yml`，每条写明安全依据
- **OSSF Scorecard**：新增评分工作流（结果发布到公开评分页并上传 code
  scanning），README 加 Scorecard 徽章——供应链安全从「自己觉得做得好」
  变成「有公开体检报告」
- 教学文件 learn-github-actions.yaml 补上最小权限声明

---

## [1.5.0] - 2026-09-11

效率与多源场景：多目标不再重复拉取源镜像，凭证支持按仓库映射，架构文档补齐三轮迭代的设计记录。

### 新增

- **多目标同步源只拉取一次**：多个目标都需要推送时，源镜像先拉到本地 OCI 目录
  中转，再逐目标推送。此前每个目标各拉一次，大镜像（几 GB 的 ML 框架）会被
  成倍拉取时间与上游限流风险拖累。中转是**纯优化**：磁盘空间预检不足、拉取失败
  都自动退化为逐目标拉取并告警，不改变任何结果记录的语义；单目标与
  `--strip-attestation` 模式不走中转。拉取耗时计入第一个目标并在备注注明，
  避免耗时排行看起来不合理
- **按源仓库映射凭证**：新增 `--src-credentials <文件>`（或环境变量
  `SYNC_SRC_CREDENTIALS`，值为文件内容），每行「host 用户名 密码」。清单混有
  多个私有源（公司 Harbor + 私有 GHCR）时不必再拆分运行；未匹配到凭证的 host
  走匿名，公开镜像不受影响。与单一凭证（`--src-username`）互斥，混用直接报错；
  格式错误的行指明行号并中止——**错误信息不回显行内容**，因为行里有密码

### 文档

- [架构文档](docs/ARCHITECTURE.md)补齐 v1.3.0 以来的机制设计记录：镜像筛选的
  「只标记不删除」、源凭证不进命令行的威胁模型、完整性校验复用跳过判定的
  原因、通知阈值选「下载历史」而非「保存状态」的权衡、history.sh 的
  Artifact 即数据源、多目标中转的「纯优化」边界。每个取舍都写明被否掉的
  方案，供新场景下复用判断。同步流程图与「关键设计决策」同步更新
## [1.4.0] - 2026-09-11

同步质量：从「成功了吗」推进到「同步得对不对、快不快、出问题时是否值得被通知」。

### 新增

- **完整性校验**：新增 `--verify`，同步成功后逐平台比对源与目标的子 manifest
  digest——`skopeo copy` 返回 0 只说明命令跑完了，不代表每个平台都完整推上去了。
  有差异时明确列出缺失的平台并判定该镜像失败；拿不到 digest 时只告警不判失败。
  默认关闭（每个镜像多两次 inspect，大清单会变慢），三个工作流均开放了对应输入
- **耗时排行**：报告与 Step Summary 末尾新增「最慢的同步记录」——一批镜像的总耗时
  几乎总是被其中一两个主导，逐个翻表格看不出来。`history.sh --slowest` 则跨运行
  汇总平均耗时，同时给出波动范围：单次超时会把平均值拉高，看范围就能分辨
  「一直慢」还是「偶尔慢」，前者才值得优化
- **告警阈值**：新增 `--notify-after-failures`，同一个镜像连续失败 N 次才通知
  （默认 1，与历史行为一致）。每次失败都响的通知很快就没有人看了——上游抖动
  占了失败原因的一大部分，重跑就好。中间成功一次即清零，单个镜像独立计数。
  连续次数从历史运行的报告 Artifact 中推算，不引入任何新的存储；拿不到历史时
  按「连续失败 1 次」处理，宁可不通知也不基于猜测误报

---

## [1.3.0] - 2026-09-11

同步私有源、按需筛选，以及一个把历次报告汇成趋势的脚本。

### 新增

- **镜像筛选**：新增 `--filter` 与 `--exclude`，用正则从清单中挑选本次要同步的镜像
  （如 `--filter 'kube-' --exclude 'apiserver'`）。清单文件因此可以保持为「期望状态
  的完整记录」，不必为了临时筛选而改动它。被排除的镜像仍会出现在结果表中并标注
  原因——**悄悄消失是最危险的**，使用者会误以为它已经同步
- **私有源凭证**：新增 `--src-username` / `--src-password`（也可用环境变量
  `SYNC_SRC_USERNAME` / `SYNC_SRC_PASSWORD`），用于同步公司内部 Harbor、私有
  GHCR 包这类需要认证的源。此前这类镜像只会在拉取阶段 401，然后被记成一次普通
  的「同步失败」，看不出是缺凭证。凭证只写进 600 权限的临时文件、经
  `--src-authfile` 交给 skopeo，**不经过命令行**——命令行参数对同机其他进程可见，
  也容易被调用方的日志语句原样打印。源与目标的凭证刻意分开配置：把目标仓库的
  凭证发往源仓库，等于把「往我仓库推送」的权限交给一个你并不信任的第三方
- **同步历史趋势**：新增 `scripts/history.sh`，把历次同步报告摊在一起看——
  「某个镜像是不是反复失败」「哪些镜像最不稳定」这类问题，单看一份报告回答不了。
  它**复用已有的报告 Artifact**，不引入任何新的存储，因此也不会给仓库留下
  持续增长的提交历史。历史中有失败记录时以退出码 2 结束，便于接进别的检查
- **`--retry-delay`**：新增该参数以固定两次重试之间的间隔。默认不指定时 skopeo
  按失败次数指数退避，这是多数场景的最优解；固定间隔是为窗口式限流的自建仓库准备的
- [排错手册](docs/TROUBLESHOOTING.md)新增两条：Docker Hub 限流（`toomanyrequests`）
  与自建 registry 的 bcrypt 密码文件问题——后者的特点是不报错，registry 启动正常
  却每次认证都失败，极易被误判成 Secret 配错了。`unauthorized` 条目补上「怎么区分
  源仓库还是目标仓库报的错」，两者处理方式完全不同，此前只写了目标仓库

### 修复

- 修复 regctl 路径的平台列表解析：`printf '%s'` 不输出结尾换行，而 `read` 读到
  没有换行收尾的最后一段时会返回非零并结束循环。后果是单平台直接失败（一段都读不到），
  多平台**静默丢掉最后一个**——表现为 arm64 没同步却毫无报错。由于
  `detect_platforms` 的输出同样没有结尾换行，**所有走 regctl 路径的同步都在丢平台**。
  同时把该路径的 `--dry-run` 输出改为复述真正会执行的参数，此前它独立拼接、与实际
  行为脱节，正是这个缺陷长期未被发现的原因
- `--retries` 在 `--strip-attestation` 模式下不再静默失效。该模式走 regctl 路径，
  而 regclient 有自己的重试策略，此前传入的 `--retries` 被完全忽略却毫无提示——
  使用者以为自己控制住了重试次数。现在会明确告警说明它不生效及原因
- `--dest-exact` 的单镜像约束改为按**筛选之后**的数量判断。此前传 5 个镜像再用
  `--filter` 收敛到 1 个也会被拒绝，等于筛选能力用不上

---

## [1.2.0] - 2026-09-11

多目标同步、对自建 registry 的支持，以及一个会真正推送镜像的 CI 集成测试。

### 新增

- **多目标同步**：`--dest` 可重复指定，一次运行把同一个镜像推送到多个目标仓库。
  每个目标独立判定成败与跳过，报告中每个「镜像 × 目标」是一条独立记录，
  因此能一眼看出是哪个目标出了问题
- **TLS 校验开关**：新增 `--tls-verify`，可对自建的 HTTP registry 关闭证书校验
- **CI 真实同步集成测试**：新增一个会**真正推送镜像**的 job，用本地 registry
  容器验证首次同步、增量跳过、内容变化后重新同步三条路径，不需要任何外部凭证。
  此前这些路径只有 dry-run 覆盖，而 v1.1.0 暴露的三个缺陷恰恰全部发生在这里
- **发布说明改为组装生成**：由 CHANGELOG 手写说明、GitHub 原生变更清单
  （PR 列表与对比链接）与可选 AI 摘要三段拼成，发布时能完整看清本次改了什么

### 修复

- 修复 registry 地址含端口时生成非法目标仓库名的问题。仓库名允许的字符集
  不含冒号，而压平规则原先只替换 `/`，会把 `localhost:5000/source/hello`
  变成 `localhost:5000_source_hello`。现在会先分离出 tag，再对名称部分
  同时替换 `/` 与 `:`——只有落在最后一个 `/` 之后的冒号才是 tag 分隔符
- 增量跳过的比对原先未沿用 `--tls-verify` 设置，导致对自建 registry
  静默失效——每次都判定为「需要同步」，跳过形同虚设

---

## [1.1.0] - 2026-09-11

可观测性与性能：同步结果通知、镜像 digest 记录，以及并发与增量同步。

### 新增

- **镜像 digest 记录**：同步报告中记录源与目标的 digest。digest 是不可变的，
  而 tag 可能被上游覆盖——不记录它，就丢失了「这次同步的到底是哪一份镜像」。
  据此可以审计、可以复现，也是排查跳过判定问题时最直接的证据
- **锁文件**：新增 `--write-lock`，把镜像与 digest 写成可直接回喂给 `--file`
  的格式，用于精确复现。即便上游重新构建了同名 tag，锁定的仍是当时那一份
- **结果通知**：新增 `--notify-webhook`，同步结束后把结果推送到钉钉 / 飞书 / Slack。
  同步多是无人值守的，失败往往要等集群拉不到镜像才发现，通知正是为了补上这个盲区。
  服务商类型按地址自动识别；通知失败只告警，不影响同步的退出码
- **并发同步**：新增 `--concurrency`，可同时同步多个镜像。批量同步是典型的
  I/O 等待型任务，串行时绝大部分时间都耗在等待上，实测 4 个镜像并发后
  总耗时从 4 秒降到 1 秒
- **超时控制**：新增 `--timeout`，为单个镜像设置超时（默认 600 秒），
  避免某个大镜像或上游抽风卡住整个批量任务。未安装 `timeout` 命令时
  自动降级为不限制，并给出安装提示
- **增量跳过**：新增 `--skip-existing`，目标仓库已有完全相同的镜像时跳过推送。
  定期同步的场景下，绝大部分镜像都会被跳过，一次运行只需几秒
- 三个同步工作流均开放了上述能力的对应输入，默认开启增量跳过与并发
- CI 冒烟测试新增覆盖：并发执行、结果顺序、非法参数拒绝

### 修复

- 修复目标地址携带 digest 时生成非法推送目标的问题。源镜像形如
  `nginx:1.27@sha256:…` 时，原先会把 `@sha256:…` 一并拼进目标地址。
  使用锁文件时源天然带 digest，这个问题会立刻暴露
- 修复结果文件的字段分隔方式：制表符属于 bash 眼中的「IFS 空白字符」，
  相邻的两个会被合并成一个分隔符。当某个字段为空时其后的所有字段会整体
  左移（表现为目标 digest 被读成空值）。改用非空白的 Unit Separator

### 变更

- 增量跳过的比较依据由「顶层 manifest 的完整 JSON」改为「各平台子 manifest
  的 digest」。前者对 mediaType、annotations 与字段顺序都敏感，源与目标
  由不同 registry 提供时会误判为不同
- 比较时排除 Windows 平台：其 manifest 在传输过程中会被重新生成、digest
  必然改变，纳入比较会让跳过永远不成立
- 单个镜像的处理改为在独立子进程中执行，结果通过带序号的临时文件汇总，
  保证并发模式下结果表的顺序仍与输入顺序一致
- 校验 `--concurrency` / `--timeout` / `--retries` 的取值，非法参数在
  同步开始前就被拒绝，而不是跑到一半才出错

---

## [1.0.0] - 2026-09-11

首次以完整的开源项目形态发布。

从「几个能跑的工作流」整理为一个结构完整的项目：同步逻辑收敛为可复用的引擎、
补齐治理文件与文档体系、引入 CI 与仓库自动化，并修复了若干会导致静默失败的缺陷。

### 新增

- **批量同步**：`images_src` 现在支持一次传入多个镜像（换行、逗号或空格分隔），
  单个镜像失败不会中断其余镜像的同步，结束后输出汇总表
- **同步结果摘要**：每次运行生成 GitHub Step Summary 表格，包含每个镜像的目标地址、
  架构列表与耗时，无需翻日志即可确认结果
- **同步报告产物**：运行结束后可下载 Markdown / JSON 格式的同步报告（Artifact）
- **本地 CLI**：新增 `scripts/sync.sh`，可在本地复现同一套同步逻辑，支持 `--dry-run`
- **统一校验入口**：新增 `scripts/lint.sh`，一条命令跑完 CI 的全部静态检查
- **输入校验**：同步前校验镜像引用格式，格式错误立即标记失败并给出原因，
  不会浪费一次网络请求
- **可配置重试**：`scripts/sync.sh` 支持 `--retries` 指定失败重试次数（默认 3 次）
- 项目文档体系：`docs/ARCHITECTURE.md`（原理）、`docs/TROUBLESHOOTING.md`（排错）、
  `docs/MAINTAINER_GUIDE.md`（维护者手册）、`docs/BACKGROUND.md`（原始教程归档）
- 开源治理文件：`LICENSE`、`CONTRIBUTING.md`、`CODE_OF_CONDUCT.md`、`SECURITY.md`、
  `CODEOWNERS`、Issue / PR 模板
- CI 工作流：`actionlint` + `yamllint` + `shellcheck` + 提交信息规范校验
- 自动化：PR 自动打标签、首个贡献者欢迎、Issue / PR 过期提醒、自动发布

### 变更

- **目标仓库地址不再硬编码**：`registry.cn-shenzhen.aliyuncs.com/nicholyx` 收敛为
  `DEST_REGISTRY` 变量，可通过仓库变量 `ALIYUNCS_REGISTRY` 覆盖
- **登录方式改为 `docker login`**：使登录地址与推送地址共用同一个变量，避免改一处漏一处
- **regctl 版本固定为 `v0.11.6`**：原先使用 `releases/latest` 下载，导致同一工作流
  在不同时间可能拿到不同版本，无法复现
- **工作流运行名称**现在包含源镜像名，便于在 Actions 列表中区分每次同步
- 移除了两个与镜像同步无关的多余步骤（`actions/checkout` 与 `docker/setup-buildx-action`）
- `platforms` 改为可通过工作流输入配置，不再写死

### 修复

- **Harbor 工作流丢失多架构**：补上 `skopeo copy --all`。此前同步 multi-arch 镜像时
  只搬运 runner 所在的 amd64 平台，arm64 用户拉取会报 `no matching manifest`
- **regctl 分支在特定输入下生成非法镜像名**：源镜像带 `docker://` 前缀时，
  目标仓库名会连同前缀一起参与替换，生成 `docker:__xxx` 这类非法引用
- **注释与实现不符**：注释写「替换 `/` 为 `__`」，实际早已改为单个 `_`，会误导后续维护者
- 修正了工作流中误导性的步骤名（登录阿里云 / Harbor 的步骤原名为 `Login to Docker Hub`）

### 安全

- 所有来自 `workflow_dispatch` 的输入不再直接插值到 `run:` 脚本，统一经 `env:` 中转，
  消除表达式注入风险
- 新增 `SECURITY.md` 说明本项目的威胁模型与漏洞报告流程
- 忽略 `.qoder/` 等编辑器本地配置，避免误提交

---

## 历史版本

以下版本早于本日志的建立，依据 Git 提交历史回溯整理。

### [0.3.0] - 2026-08-29

#### 修复

- 用 `regctl index create` 重建不含 attestation 的镜像索引，
  规避阿里云 ACR 对 OCI 1.1 空 blob 的 `unknown manifest class` 拒绝
- `regctl --platforms` 需重复传参而非逗号分隔
- 改用 GitHub Releases 下载 `regctl`（原先的安装方式已失效）

### [0.2.0] - 2026-08-29

#### 新增

- 支持同步 multi-arch 镜像（amd64 / arm64）
- 增加剔除 attestation manifest 的同步选项

#### 修复

- 修正目标仓库名中斜杠替换导致无法上传到阿里云的问题

### [0.1.1] - 2025

#### 新增

- 新增 Harbor 仓库同步工作流

### [0.1.0] - 2024-06-26

#### 新增

- 首个版本：基于 `skopeo` 将国外镜像同步至阿里云容器镜像服务

---

## 版本说明

- **Unreleased** 段落由贡献者在 PR 中随手补充，维护者在发布时归入正式版本号
- 每个版本的分类固定为：`新增` / `变更` / `弃用` / `移除` / `修复` / `安全`
- 破坏性变更会在标题后标注 **BREAKING**

[Unreleased]: https://github.com/nicholyx/action-sync-images/commits/main
