# 更新日志

本文件记录本项目的所有重要变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [Unreleased]

## [1.19.4] - 2026-09-23

### 修复

- **缺少目标参数时的报错文案漏了 `--dest-keep-path`**（[#129](https://github.com/nicholyx/action-sync-images/issues/129)）。`--dest-keep-path` 是与 `--dest` 并列的第三种目标模式，但那条报错只说了「`--dest` 或 `--dest-exact`」——**同一份文件里另外两条同类文案都是对的**，只有这一处漏了。撞上它的人恰恰是最需要它说对的那一类：想把路径结构保留到 Harbor 类仓库、又忘了带前缀参数的，会被告知去用 `--dest`（压平，正是他不想要的）或 `--dest-exact`（只接受单个源镜像）。文案现已与同文件既有写法一致，`docs/ARCHITECTURE.md` 里照抄该文案的流程图一并修正
  - 这条文案此前**没有任何测试覆盖**：原断言只查退出码，不看文案。现已补上，且**只查报错那一行**——`log_error` 之后紧跟着打印 `usage`，而 `usage` 本来就列出了全部参数，拿完整输出去 grep 无论文案写得多烂都会命中（实测对照：三条 flag 在完整输出里全部命中）。这是「恒真断言」的又一具体形态

- **`./scripts/lint.sh` 补上 zizmor，并如实划出它覆盖不到的那半**（[#130](https://github.com/nicholyx/action-sync-images/issues/130)）。五处文档（两个 README、`AGENTS.md`、`CONTRIBUTING.md`、脚本自述）都写着它「一键跑完 CI 的全部静态检查」，而它实际缺少 **zizmor** 与**提交信息规范**——`CONTRIBUTING.md` 据此写「提交前跑一次，能省掉一轮 CI 返工」，**省不掉**：使用者按文档认为提交信息已验过，用一个不合 Conventional Commits 的标题开 PR，`commit-messages` job 当场红。现在两半分开处理：**能 100% 本地复现的补上，复现不了的写清为什么**
  - **zizmor 按 CI 的同一条命令接进来**：`docker run --rm -v "$PWD":/repo:ro <镜像> /repo --no-online-audits`。镜像引用从 `ci.yml` 抽，不写第二份——写死会在 CI 升级时静默漂移，而漂移正是这个 issue 本身要消除的那类问题。本机没有 docker 时退化为本机 zizmor（版本未必与 CI 对齐，打印提示），两者都没有才跳过；**镜像拉取失败则报失败**，那是「工具在但没跑成」，与「工具不在」不是一类
  - **提交信息规范不接，改文档说清为什么不接**。CI 的 `commit-messages` job 校的是 PR 里的提交与 **PR 标题**，而标题在 PR 建立之前根本不存在，本地任何入口都验不了它——接上 `--last` 只能关掉一半，而「一半」支撑不起「已在本地验过」这句话。更要紧的是它会引入一个**假绿来源**（实测：`--range nonsense..HEAD` 得到「全部合规（共检查 0 条）」且 exit 0，`--last` 的 `HEAD~1..HEAD` 在仓库首个提交上同理）。在「宁可不给结论、也不给错结论」的项目里，拿更贵的东西换一句更响亮的承诺并不划算，所以这里是**让承诺缩小到真话**：覆盖项与不覆盖项都写进文档，脚本结尾也明说「本地绿不等于 `commit-messages` 会绿」
  - 抽取版本号失败（`ci.yml` 里没有 pin 的镜像引用）同样**报失败并说明原因**，不静默跳过；顺带补全 `docs/MAINTAINER_GUIDE.md` 里 `ci-summary` 的覆盖清单（漏了 zizmor 与集成测试）

- **`--file` 清单末尾没有换行时最后一行被静默丢弃**（[#143](https://github.com/nicholyx/action-sync-images/issues/143)）。`collect_images` 读清单用的是 `while IFS= read -r line`，而 `read` 读到**没有换行符收尾的最后一段**时返回非零（表示遇到 EOF 而非完整行），循环体一次都不执行。后果有两种形态，**两行的那种最坏**：清单只同步第一行、退出码 0、日志照旧打印「同步完成」，使用者以为两个都同步了；单行的清单则整段读不到，落到「没有提供任何源镜像」并以 1 退出——明明提供了。修法照仓库既有范本加 `|| [[ -n "$line" ]]`（`parse_src_credentials`、`parse_lockfile` 早就是这个写法，本条规则见 `.trellis/spec/engine/bash-rules.md` 的 printf 那节，regctl 路径曾因它静默丢平台）
  - **同族位置逐个核对了**：`scripts/` 下 21 处 `while IFS= read`（清单里列的 18 处，另加 `scripts/lint.sh` 2 处、`scripts/check-commit-msg.sh` 1 处）里只有这一处有风险；`.github/workflows/ci.yml` 另有 2 处（CI 断言自身的 `<<<"$out"` 读取），同样安全。全仓 23 处里就这一处有风险——它读的是使用者手写的清单，是唯一可能不带结尾换行的一类。其余各处的写入方都保证换行收尾，逐处有据：`printf '%s\n'`（`split_images`、`--platforms` 拆分、去重与 host 去重、耗时排行）、`sort`（文件名收集）、`git log --format`（不带 `format:` 前缀时按 `tformat:` 处理，每条之后都补换行——git 文档明写，不是实测碰巧）、`gh run list --json --jq`（实测输出以 `0x0a` 结尾）。`--src` 路径经 `split_images` 的 `printf '%s\n'`，同样不受影响。全仓 `done < 文件` 的读取只有 5 处，无一处遗漏：清单（本次修）、凭证文件与锁文件（早已带同一保护）、`notify-history.tsv`（由本脚本的 `printf '%s\n'` + `sort -o` 写出，见 `fetch_sync_history`）、`history.sh` 的 `run-list.txt`（由 `gh run list` 写出）。**没有为了凑数去改其余各处**——加保护会掩盖「输入本该合规」这个事实
  - 断言写进 CI 的「验证 --file 清单解析」：三种形态（有结尾换行 / 无结尾换行 / 单行无结尾换行）必须解析出**同样的结果**。这是刻意的表述——写成「无结尾换行时同步 N-1 个」等于把缺陷固化成契约。**变异验证**：单独去掉这一处保护（不连带删掉配套语句），后两条断言当场变红、第一条照旧绿，正好说明带结尾换行的那条单独存在时等于没测
  - `docs/USAGE.md` 的清单格式说明点明「末尾的换行不是必须的」——免得有人以为必须

## [1.19.3] - 2026-09-23

### 修复

- **regctl 下载失败不再一次就放弃**（[#122](https://github.com/nicholyx/action-sync-images/issues/122)）。`ensure_regctl` 从 GitHub release 取 regctl，此前一次失败就 `die`——而同一份代码里其他网络操作都遵循「重试一轮、固定间隔、不做参数化」的口径（`history.sh` 的报告下载与 `gh run list`）。问题在集成测试补上 regctl 路径之后被放大：CI 的 runner 环境是干净的，每次运行都要下载一次，脚本没有第二次机会，一次网络抖动就整条流水线红。现在失败会重试一轮（间隔 5 秒），重试成功后打印「重试成功」，仍失败则报「已重试一次」并保留下载 URL
  - **下载改为先落临时文件、成功后原子替换**到目标路径。`curl -o` 失败会留下**不完整文件**，而重试让「同一个固定名字被写两次」成为常态——半成品留在 `PATH` 前置目录里，会被后续运行或使用者的手工调用撞上。反过来，临时文件方案下 `chmod +x` 得到的是 **711** 而非改动前的 755，因此改为显式 `chmod 755`：成功路径的行为与权限都不该顺带改变
  - 下载期间的中断也有人管了：`cleanup` 的 trap 要到创建 `WORK_DIR` 时才安装，而 `ensure_regctl` 跑在它之前，光把它登记进 `cleanup` 覆盖不到那一段（尤其是重试的 5 秒等待）
  - 顺带修掉同族的一处：`--check-updates` 与 `--audit-lock` 模式下 `--strip-attestation` **已经**被告警「本次不生效」，但 `ensure_regctl` 照旧执行——于是平白下载一次 regctl，在没有网络的环境里还会以「下载失败」退出 1，**而它压根不需要这个工具**

## [1.19.2] - 2026-09-23

### 修复

- **`--strip-attestation` 下 `--tls-verify false` 与源仓库凭证不再被静默忽略**（[#120](https://github.com/nicholyx/action-sync-images/issues/120)）。regctl 路径（`sync_via_regctl`）此前只构造 `regctl index create <dest> --ref <src> --platform …`，既不传 TLS 也不传凭证——而这两样在 skopeo 路径上都有。后果分两类：自建 HTTP / 自签仓库的使用者**根本用不了**这个模式（报 `server gave HTTP response to HTTPS client`，尽管参数已显式传了）；配了 `--src-username` / `--src-password` / `--src-credentials` 的私有上游会拿到 `no credentials available: unauthorized`，**而日志里还照常写着「源仓库凭证已装载」**——把排查引向完全错误的方向。两者都属于「显式传入却不生效」，正是本项目明确定为比直接报错更危险的那种失败
  - **TLS 走 `--host reg=<host>,tls=disabled` 逐命令注入**，不写配置文件：`--host` 是**叠加**语义，使用者在 `~/.regctl/config.json` 里给别的 host 配的 `cacert` 与凭证原样保留；`REGCTL_CONFIG` 则是**替代**语义（实测：指向另一份配置后原 host 全部消失），用它等于为一个参数把使用者的全部 registry 配置作废
  - **凭证走临时 `DOCKER_CONFIG` 目录**，其中的 `config.json` 是使用者的 docker 配置与 `SRC_AUTHFILE` 的**递归合并**。必须合并而不是直接指向源凭证所在目录：`DOCKER_CONFIG` 同样是替代语义，只放源凭证会让使用者**目标仓库**的 `docker login` 凭证消失——「修好了源、弄坏了目标」，比原缺陷更隐蔽（原缺陷至少在日志里留下一个 401）。目录 0700、文件 0600，退出时清理
  - **凭证不进命令行**，沿用项目在源凭证构造处的既有原则（命令行参数对 `ps aux` 可见，且 `--dry-run` 会原样打印命令）；dry-run 如实复述新增参数，只报凭证目录位置、不报内容
  - **`--tls-verify false` 在这条路径上映射为明文 HTTP**。regctl 的 `tls` 是单值（`disabled` 管明文 HTTP，`insecure` 管自签证书，二者不可兼得），而 skopeo 的 `--tls-verify=false` 同时覆盖两种场景。选 `disabled` 是因为 regclient 对「连不上」给出的官方建议就是 `--tls disabled`；代价是**自签 HTTPS 仓库在这条路径下不适用**，显式使用该组合时会打印一条说明并指向 `~/.regctl/config.json` 的 `cacert` 用法。Docker Hub 的三个别名（`docker.io` / `index.docker.io` / `registry-1.docker.io`）不注入——regclient **认**这个名字，会把 `tls=disabled` 套到映射后的 `registry-1.docker.io` 上。这不会直接失败（实测明文请求拿到 301、跟到 HTTPS 后照常完成），但会多一次明文请求，并把「出站 80 端口可用」变成 Docker Hub 源的前提；修复前它走的是默认 HTTPS，所以这里保持原状
  - 集成测试补上私有源用例：`registry:2` + htpasswd 起一个要求认证的仓库，**同时**断言「不带凭证必须失败」与「带凭证必须成功」——只测后者的话，源恰好匿名可读时断言会恒真。原来那段手工写 `~/.regctl/config.json` 的环境准备据此删除，并反过来**断言它不存在**：留着它就会替脚本兜底，让用例即使把 `--tls-verify` 传丢了也照样通过

## [1.19.1] - 2026-09-22

这一轮补的是**覆盖**：regctl 路径的真实推送此前从未进过 CI——而项目自己记过，v1.1.0 的三个缺陷全部发生在 dry-run 覆盖不到的真实推送路径上。补上之后它立刻暴露了两个被静默忽略的参数与一处不一致的重试口径。

### 变更

- **regctl 路径（`--strip-attestation`）的真实推送进了 CI**：`sync_via_regctl` 是与默认 skopeo 路径**完全不同的实现**（`regctl index create` 重建索引、逐平台复制），但集成测试 job 里此前一次都没走过——它只被 dry-run 与参数层面的用例覆盖，而真实推送恰恰是 dry-run 到不了的地方。新用例在本地 `registry:2` 上真推送，断言「产物的平台集合**恰好**等于 `--platforms` 指定的那个」与「产物摘要与源不同（即真的重建了索引，而非原样搬运）」。第二条是关键：只断言「推送成功」是不够的——skopeo 路径同样会成功
  - **源换成多平台索引**：原计划复用既有的 `source/hello:latest`，但它是用 `skopeo copy` 准备的，而后者默认 `--multi-arch system`——落到 registry 里其实是**单平台 manifest**。拿单平台源测「只保留指定平台」等于什么都没测（regclient 对非索引的 `--ref` 本来就忽略平台过滤），用例因此自备一份 `--all` 的索引——顺带带上上游的 attestation manifest，正是这个选项现实中的用武之地。另加一条前置断言，把「源退化成单平台」挡在断言恒真之前
  - **不预装 regctl**：脚本的 `ensure_regctl` 本就会从 GitHub release 下载到 `$HOME/.regclient/bin`，用例走的就是这条真实路径，顺带覆盖它本身；预装反而绕开了被测代码
  - **本地测试仓库要告诉 regctl 它是 HTTP**：regctl 默认走 HTTPS，且不像 skopeo 那样「TLS 失败就退回 HTTP」，而 `sync.sh` 目前没有把 `--tls-verify` 的值传给 regctl，因此用例按 regclient 文档把这台本地 registry 标成 `tls: disabled`。这是环境准备而非被测行为的替代，但它同时暴露了一个真实缺口：**`--tls-verify false` 在 `--strip-attestation` 模式下被静默忽略**，自签 HTTPS 仓库的使用者会撞上。**源凭证也一样**——`--src-username` / `--src-password` / `--src-credentials` 同样到不了 regctl（regclient 的入口是 docker config 或它自己的 host 配置），用这些参数配私有上游的人会拿到一个 `no credentials available: unauthorized`，而日志里还写着「源仓库凭证已装载」。按本项目「显式传入却不生效必须告警」的既有规矩（`--retries` / `--retry-delay` 已有先例），这两条至少都该有告警——另开任务处理

## [1.19.0] - 2026-09-22

这一轮做的是**对账**：文档说的与实现是否还一致。「改了行为要同步文档」是既有规范，但反方向的漂移不会自己暴露——文档停在旧版本，而使用者照抄就会撞上。

### 变更

- **参数速查表补全**：三个工作流各有一个 `verify` 与 `notify_after_failures` 输入，USAGE 里也都有专门章节说明它们，但六个参数表（中英 README + USAGE 三处）都没有列——只读参数表的使用者不知道这两个能力存在。这不是「功能未文档化」，而是「**查表查不到**」；补全后用机械检查复核：从工作流 YAML 抽 `inputs` 块与文档表格逐项比对，六个表全部对上

## [1.18.0] - 2026-09-22

这一轮补的是**第一道坎**。默认目标仓库是项目作者本人的命名空间，而文档把它说成了可选项——使用者会在第一次同步时撞上它，或者更糟：在体检时拿到一个「全部最新」的假绿结论。

### 变更

- **默认目标仓库是项目作者示例用的命名空间，这件事现在在第一次同步之前就可见**：三个工作流的默认目标是 `registry.cn-shenzhen.aliyuncs.com/nicholyx`（项目作者的阿里云命名空间），但此前只有工作流里一行注释提到它——README 的快速开始三步完全没提这个变量、USAGE 把「更换目标仓库」放在**可选**一节、TROUBLESHOOTING 的 `denied` 排查还把 `nicholyx` 当成「该去创建的命名空间」来举例。使用者既不知道自己推到了谁的命名空间，也想不到问题出在默认值上
  - **运行时提示**：三个工作流在登录目标仓库之前判断「目标仍是内置默认值**且**仓库 owner 不是作者」，命中则打印一条 `::warning::`（说明要同步到自己的仓库就设 `ALIYUNCS_REGISTRY`，确实在用则可忽略）。判据是**精确比较**目标地址与默认值字面量，**不是**「值里含 nicholyx」——后者会误伤把区域换到杭州、却保留了同名命名空间的使用者
  - **只提示、不阻断**：阿里云的命名空间是**账号内唯一**而非全局唯一，使用者在自己账号下建一个同名的 `nicholyx` 照样能推成功，所以默认值**可能是合法选择**，报错会误伤。退出码与同步行为零变化；作者自己的仓库下不出现该提示（用 `github.repository_owner` 判断，不靠字符串猜）
  - **文档与输入描述口径一致**：README（中英）的快速开始新增「设置目标仓库」一步（后续步骤顺延），第 4 步的拉取示例也注明「把 `nicholyx` 换成第 2 步设的命名空间」；USAGE 的「可选：更换目标仓库」改为「目标仓库（`ALIYUNCS_REGISTRY`）」并移入准备凭证的必读区；TROUBLESHOOTING 的 `denied` 排查第一条改为「确认目标仓库是你的」；三个工作流的 `dest_registry` **输入描述**也补了一句——那是点 Run workflow 时最早看到的文案。各处都说清**默认值是谁的**与**怎么改**，也都如实写「不设置也能跑，但会推到作者的命名空间」——不夸大成「必定失败」
  - 顺带修掉 `docs/TROUBLESHOOTING.md` 里两个点不开的锚点（既有问题，非本版引入）：标题 ``错误：`platform ... not found` `` 的 slug 是 `错误platform--not-found`（两个连字符，中间的 `...` 去标点后留了个空段），而链接写的是一个
  - **`Check-Registry` 那一处危害更大，所以一并修**：同一个默认值，同步路径上推送会被 `denied`（能看见），而体检的对象**就是** `DEST_REGISTRY`——作者那边镜像齐全时报告显示「全部最新」，使用者自己的仓库其实是空的。体检给的是**结论**，这个结论是假绿；它的提示因此措辞不同（「本次结果描述的是那个仓库，不是你的」），且只在 `audit` 模式出现（`updates` / `lock` 根本不用目标地址，报警会是噪音——该工作流的登录步骤本就 `if: inputs.mode == 'audit'`）
  - **不改默认值**：改成空值会让作者自己的运行失败，改成别的同样是把问题推给下一个人；亦不改 Harbor 工作流——它已是「必填 + 缺失即报错」，是本次对齐的范本

## [1.17.0] - 2026-09-20

这一轮补的是**记录**。路线图上的功能与缺陷已在前几轮清空，而 v1.14–v1.16 积累的六条设计决策还只散落在代码注释、CHANGELOG 与 PR 描述里——它们恰恰是「看起来可以优化、但当初不做成那样有原因」的那类地方。

### 变更

- **架构文档补记 v1.14–v1.16 的设计决策**：`docs/ARCHITECTURE.md` 的「关键设计决策」一节此前停在 v1.11，这四轮改动里积累的取舍只散落在代码注释与 PR 描述中。补六条「为什么这样而不是那样」——失败原因为什么要进报告与通知、报告下载的重试为什么放在主循环之后且只重试一轮、`gh run list` 的失败为什么重试一轮再报错、dry-run 的判据为什么是「有没有真的搬过」、锁文件与通知为什么是「不生效 + 告警」而不是产出「dry-run 版」、报告为什么加 `dry_run` 字段而不是改 `status`。每条都对照代码注释、CHANGELOG 与 PR 正文核过；这些正是「看起来可以优化、但当初不做成那样有原因」的地方，而这一节存在的意义就是**改设计前先确认不是把记录过的决定改回去**

## [1.16.1] - 2026-09-20

v1.16.0 清掉了 dry-run 留下的「痕迹」（耗时、锁文件、通知、目标 digest），这一版清的是最后一处、也是唯一会**改变别人判断**的一处：它的报告被当成真实运行，进了趋势与连续失败计数。

### 修复

- **`--dry-run` 产出的报告不再被历史聚合当成一次真实运行**：dry-run 的报告里每条镜像都是 `status: success`（`sync_to_dest` 直接返回），但它描述的是**计划**，不是已发生的事。此前两条消费路径都不区分它：`history.sh` 的趋势把它算进「累计同步 N 个镜像次」；更严重的是 `sync.sh` 的 `fetch_sync_history` 把它混进连续失败计数的数据源，而 `count_consecutive_failures` **遇到任何非 failed 记录即清零**——一次没搬过任何东西的干跑足以压掉一个真实存在了 N 次的失败序列本该触发的告警（`--notify-after-failures`）。这是 v1.15.1 修掉的「坏报告让计数偏小」的同类后果（都是把该响的告警压掉），但成因完全不同：那次是解析失败被静默丢弃，这次是数据本身被误读。现在 `sync-report.json` 顶层新增 `dry_run` 字段（boolean，`generated_at` 之后、与 `dest_registry` 同为「这份报告是怎么来的」的元信息），两个消费方据此跳过
  - **跳过必须可见**：`history.sh` 告知跳过了几份并说明原因（它们描述的是计划，不是已发生的同步），与 v1.15.1 处理坏报告的口径一致；全部报告都是干跑时，报错也说清是哪种「空」——原来只有「没有 JSON 报告」与「没有一份能解析」两种说法，而「能解析但都是干跑」与「没有一份能解析」是完全不同的两回事，混成一句会让人去删根本没坏的文件
  - **过滤在「准备文件列表」层完成，4 处聚合函数一行未改**：一次分类、四处受益、告知也在一处。仍沿用 v1.15.1 已加的逐文件预检——把「能不能解析」扩成「能解析且是真实运行 / 能解析但是干跑 / 解析不了」三分类，仍是每个文件一次 jq 调用，不是两次
  - 顺带修掉三分类判据带来的一个既有缺陷：**合法 JSON 但不是对象的文件**（数组、标量，比如误放进 `--dir` 的其它 JSON）此前能过预检、被当成报告送进聚合，让 `jq` 在渲染时直接报错并泄漏它的退出码；现在归入「无法解析」跳过并告警，其余报告照常聚合。这一类里最阴的两个是 **`null` 与空文件**：jq 里 `null.foo` 是 `null` 而**不是错误**，空文件则**不产出任何值且退出码为 0**——判据若不显式写明「顶层必须是对象」并保留 `-e`，它们会被静默当成真实报告（`null` 让运行次数显示成 `共 2 次运行`，时间范围里还多一个 `—`）。这正是本版要消灭的「虚高」换了个入口，所以 CI 断言不只看「被告知跳过」，还断言「运行次数只算真正的那一份」
  - **兼容性是硬约束**：旧报告没有该字段，视为真实运行——升级不该让历史数据失效；`--check` / `--audit*` 的检查报告同样走「真实」分支，行为不变。非 dry-run 报告的聚合结果与退出码语义**逐字不变**（含 `--check` 三条路径）
  - dry-run 报告**照常落盘**：它是「计划」，有独立价值（给人看）。本版只改「历史聚合要不要算它」，不改它的其他字段——`status` 仍是 `success`，改它会波及全部消费方，而「加一个可区分的标记」已经足够回答问题

## [1.16.0] - 2026-09-20

本轮主题是 **dry-run 的诚实性**。前几轮清掉的是「报告说的不是真的」（v1.15.0：json 转义、失败原因、运行列表失败）与「坏数据让输出失真」（v1.15.1），这一轮清的是「**预告**说的不是真的」——`--dry-run` 是唯一一个「预告」形态的输出，所以它的不诚实最容易被当成事实：一句「镜像同步完成…失败 1」的通知，收件人会去排查一件根本没发生的事。

### 变更

- **`--dry-run` 不再留下「做过」的痕迹**：它没有真的搬过任何东西，但此前有四处输出说着相反的话。统一判据是「**有没有真的搬过**」，不是「这段代码跑了多久 / 这个函数有没有被调用」。
  - **耗时如实为 0**：计时区包住的是 `sync_to_dest`，而 dry-run 下它只打印一行命令就返回——测到的是「打印一行花了多久」。微秒级够快，只有在负载高的 runner 上才会跨过 1 秒边界，于是 `1s` 偶发地进报告 json、md 与 Step Summary 的耗时列，并让「耗时全为 0 时排行应隐去」那条断言偶发误报（2026-09-19 曾在一次 PR 上把整个 smoke-test job 带崩，重跑即绿）。现在 dry-run 的 `images[].seconds` 恒为 0，「最慢的同步记录」也随之必然隐去
  - **不写锁文件**：锁文件头部写着「可直接用 `--file` 读取本文件实现精确复现」，而这次运行什么都没推——在能拿到源 digest 的环境里它看起来完全合法，拿它回喂 `--file` 会让人以为复现的是「当时推上去的那一份」
  - **不发送通知**：通知正文写着「镜像同步完成……失败 N 个」，收到的人会去排查一次**根本没发生**的同步。它是四处里唯一主动推送给他人的出口，也是危害最大的一处
  - **不记录目标 digest**：`dest_digest` 由查询**目标仓库**得到，dry-run 下查到的是**上一次真实同步**留在那个 tag 上的镜像，写进报告的「目标 Digest」列会被读成本次同步的产物。`source_digest` 保留——它是源镜像自身的属性，与本次是否推送无关
- **同步模式下，`--dry-run` 与 `--write-lock` / `--notify-webhook` 同用时，后两者不生效并显式告警**：与 `--audit-lock` 忽略无关参数的既有做法一致，两个参数合并成一条告警、一次列全，并说明怎么办（去掉 `--dry-run`）。只在参数真被传入时告警——默认值不生效不打扰。只读检查模式（`--audit` / `--check-updates` / `--audit-lock`）不受影响：那里 `--dry-run` 本就按既有语义忽略，通知照常发出
- **非 `--dry-run` 的真实同步行为完全不变**：耗时照常计时、锁文件照常写、通知照常发、目标 digest 照常查；退出码语义不变

## [1.15.1] - 2026-09-20

v1.15.0 修好了「新报告不再产坏 JSON」，这一版管过去：artifact 保留期内仍存在的旧坏报告，不再让消费方整体崩掉或静默丢弃。

### 修复

- **坏掉的历史报告不再让分析整体失效，也不再被静默丢弃**：v1.15.0 之前产出的报告可能是不合法 JSON（镜像名含 `"` 时裸 `printf` 拼接的产物，[#103](https://github.com/nicholyx/action-sync-images/issues/103)），而 artifact 保留期（默认 30 天）内这些文件仍然在——修好了未来，过去还没人管。两条消费路径此前各有各的坏法：`history.sh` 用 `jq -s` 一次读整批，**一份坏则全废**，且报错不指明是哪个文件，只能自己二分查找；`sync.sh` 的 `fetch_sync_history` 是 `2>/dev/null || true` **完全静默**，少算的历史让「连续第 N 次失败」偏小，**该响的失败通知不响**。现在两处行为一致——跳过、计数、告警里给出文件名；`fetch_sync_history` 还额外说明后果（「连续失败次数的判断可能偏小」），因为「跳过 3 份」是事实，「判断可能偏小」才是使用者需要据此调整的信息。`--check` 模式下「无法解析」与「类型不匹配」两种跳过原因分开告警（前者在预检阶段拦下，**先于**按 `.check` 字段过滤——否则会被 filter 的吞错掩盖），使用者能分辨该删文件还是该换 `--check`。全部报告都坏时明确报「没有一份能解析」并指出首个坏文件，不再把裸的 `jq: parse error` 抛给使用者。判据只到「是不是合法 JSON」为止，**不做结构校验**：报告 schema 会演进，旧报告缺新字段是正常的，判成「坏」会让升级本身变成一次数据失效。**退出码语义不变**：坏文件属「无法判定」，不触发 2；「全部报告都坏」时的退出码从 5 变为 1——5 本来是 `jq` 退出码的泄漏（不在脚本文档化的 0/1/2 体系里），1 与「目录里没有 JSON 报告」同口径（[#108](https://github.com/nicholyx/action-sync-images/issues/108)）

## [1.15.0] - 2026-09-19

本轮主题是「失败之后」：既要知道**怎么办**（可粘贴的重跑指引），也要知道**为什么**（原因进报告与通知），而说出口的话本身必须可信——三处「输出说的未必是真的」在同一轮清掉。

### 新增

- **失败原因进入报告、Step Summary 与通知**（[#102](https://github.com/nicholyx/action-sync-images/issues/102)）：`R_NOTE` 此前只在终端表格里打印一次，同一份运行「终端知道原因、报告不知道」——三种只读检查的报告都带 `note`，唯独同步主模式没有。现在 `sync-report.json` 的 `images[]` 新增 `note` 字段（始终存在，无原因时为空字符串），报告 md 与 Step Summary 新增「说明」列（空值显示为 `—`），结果通知的「失败详情」每行带出该镜像的原因。于是「改输入」与「重跑」在报告里就能分辨——这正是重跑指引已经在做的区分，此前缺了报告这一环。只影响输出，不改搬运行为、退出码与原因的产生逻辑
- **重跑指引**：同步失败后，Step Summary 与落盘报告里直接给出**可粘贴的失败项清单**——`Sync-Images-to-AliYuncs` / `Sync-Images-to-Harbor` 给镜像列表（粘进 `images_src` 即可只重跑失败项），`Sync-Batch` 给锚定 filter 正则（粘进 `filter` 输入即可）。正则以 `^…$` 锚定并转义 ERE 元字符，不会把 `nginx:1.27-alpine` 这类同前缀 tag 一并带进来。「镜像引用格式错误」这类重跑也不会成功的项被排除在外，并用一句计数交代，不静默消失。报告 json 新增机器可读的 `rerun` 字段。全绿与 dry-run 时该节不出现

### 修复

- **`sync-report.json` 的 JSON 转义**（[#103](https://github.com/nicholyx/action-sync-images/issues/103)）：json 报告此前由裸 `printf` 逐字段拼接，镜像引用含 `"`、`\` 或控制字符（如 tab）时会**静默产出非法 JSON**——终端照常显示成功，从同步到落盘全程零报错，直到 `history.sh` 解析时才炸，排查时不会联想到「某次同步的镜像名里有引号」。现在改由 `jq` 构造，与三类只读检查报告收拢到同一条路径；字段名、层级、顺序完全不变，类型也锁死：计数类与 `seconds` / `rerun.not_rerunnable` 保持 number，`strip_attestation` 保持 boolean，无失败项时 `rerun.images` 仍是 `[]`
- **`history.sh` 取运行列表的失败不再冒充「没有运行记录」**（[#98](https://github.com/nicholyx/action-sync-images/issues/98)）：进程替换 `< <(gh run list … || true)` 既拿不到退出码、又把失败吞掉，网络 EOF 时得到空输出，于是和「这个仓库确实没跑过」混成同一种结果，报错还引导人去查「工作流是不是没跑过」——正是 #87「下载失败冒充无附件」的同族问题。现在 stdout 与 stderr 分开落文件、接住退出码，失败先重试一轮（间隔 5 秒，与报告下载的重试同口径），仍失败则如实报「获取运行列表失败（已重试一次）：<gh 的真实错误>。多为网络原因，稍后重跑即可」；**真正空列表的文案逐字不变**，也不触发重试。退出码语义不变。

## [1.14.0] - 2026-09-16

本轮主题是网络韧性：瞬时抖动导致「有附件但下载失败」时自动挽回一次，不必自己重跑整条命令。

### 新增

- **报告下载的批量重试**：瞬时网络抖动导致「有附件但下载失败」时，`history.sh` 在主循环结束后自动重试一轮（其余运行先下载完天然形成退避，失败项之间再固定等 5 秒），重试挽回的直接计入成功——不再需要自己重跑整条命令。只重试一轮（持续抖动属极端环境，如实报错 + `--dir` 兜底）；无附件与附件已过期的运行不重试

## [1.13.0] - 2026-09-16

本轮主题是动手之前先看到将要发生什么：`--dry-run` 从「打印命令」升级为「先给计划」。这也是项目的第一个外部功能贡献。

### 新增

- **dry-run 同步计划预览**（[@liang-zhenxiang](https://github.com/liang-zhenxiang) 的首次贡献，[#94](https://github.com/nicholyx/action-sync-images/issues/94)）：默认同步模式在 `--dry-run` 时先输出筛选后的源镜像、目标数量、执行路径、平台策略与源 → 目标映射，并写入 GitHub Step Summary。计划复用同一套目标解析，不预测 `--skip-existing` 结果，也不虚构未显式指定的平台；只读检查模式保持原有 dry-run 忽略语义

## [1.12.0] - 2026-09-16

本轮主题是文档同步：把 v1.8~v1.11 落地时的设计决策补进架构文档，README 路线图跟上——设计记录的价值在于改设计前先确认不是把记录过的决定改回去。

### 变更

- **架构文档补齐 v1.8~v1.11 的设计记录**：新增六个「为什么」——锁文件时效审计为什么查上游而不是比对本地、检查报告为什么要落盘、lock 校验为什么上云、`--check` 为什么做趋势而 `check-updates` 明确不做、趋势落盘为什么 md + json 双份且聚合口径要单一来源、下载失败为什么与「无附件」区分。每个小节的论断逐条对照代码核过，被否方案以历史 PR 正文与代码注释为准
- **README 路线图补齐**：补 v1.9.0（检查报告趋势聚合）与 v1.10.0（趋势落盘与 History-Trend 工作流）条目

## [1.11.0] - 2026-09-15

本轮主题是趋势下载可靠性：网络抖动导致的附件下载失败不再被误报为「没有任何附件」，趋势取不到数时能如实指出原因。

### 修复

- 修复 `history.sh`：报告附件下载失败（网络抖动）被误报为「这 N 次运行里没有任何附件」，误导排查方向——附件明明存在；现在区分「无附件」（正常）与「有附件但下载失败」（如实计入汇总并提示可重试），附件已按保留期清理的场景同样如实说明

## [1.10.0] - 2026-09-14

本轮主题是趋势可用性：修复 `--check` 下载模式取不到检查报告的取数窗口缺陷（体检与同步趋势同时受益），并让趋势结果可落盘、可在 Actions 页面直接查看。

- **`history.sh --check` 的下载模式取不到检查报告**：取数窗口用的是「最近 20 次
  运行」且不按工作流过滤，而体检工作流只有手动触发——窗口几乎总被 CI 等高频
  运行挤占，结果是「一份报告都捞不到」（这不是数据不存在，是取数范围选错了；
  同步趋势的下载模式同样受影响，只是同步跑得更勤、暴露得晚）。现在下载模式
  支持 `--workflow` 过滤，`--check` 默认只看体检工作流（`Check-Registry`）的
  运行，相关提示文案也按模式区分
- **趋势结果落盘并上 Actions 页面**（`history.sh --report-dir` +
  `History-Trend` 工作流）：趋势查询此前只有装了 CLI 的本地入口，结果只进
  stdout——不能归档，也不能给别的系统消费。现在 `--report-dir` 把趋势落盘为
  `.md`（与 stdout 同源）与 `.json`（`generated_at` / `query` / `summary` /
  `rows`，机器可读）；`History-Trend` 手动工作流在 Actions 页面就能跑趋势，
  结果进 Step Summary 并上传 Artifact，红绿与体检同一套（绿 = 窗口内没有
  需要处理的记录）。聚合口径单一来源：渲染与落盘共用同一组聚合函数

## [1.9.0] - 2026-09-13

本轮把审计数据用起来：锁文件时效校验有了云端入口，检查报告能聚合趋势——「哪个镜像一直落后 / 一直在漂移」从单次报告里的猜测，变成窗口内可回答的问题。

- **检查报告的趋势查询**（`history.sh --check audit` / `--check lock-audit`）：
  把历次检查报告摊在一起，回答「哪个镜像一直落后 / 一直缺失」「哪个锁条目
  一直在漂移」——单次报告只讲「这一次」，而漂移和落后恰恰要看「是不是一直」。
  数据复用 v1.8.0 起落盘的检查报告（体检工作流的 `check-report` Artifact），
  不引入新的存储。分组键是「源镜像 + 目标」（审计状态绑定目标仓库）；
  退出码 2 只由确定异常的历史触发，「无法判定」不触发（查不成不等于出问题，
  但在表格中独立可见）。`check-updates` 不支持趋势：未收录 tag 的增减没有
  趋势价值，显式传入得到参数错误
- **体检工作流支持锁文件校验**（`Check-Registry` 的 `mode=lock`）：锁文件时效
  校验（`--audit-lock`，v1.8.0）此前只有本地入口，现在在 Actions 页面就能跑——
  传入锁文件路径即可，报告与其他检查一样落盘上传（`check-report` Artifact），
  为「哪个镜像一直在漂移」的趋势积累云端数据。工作流新增 `lock_file` 输入
  （默认 `images.lock.resolved.txt`）；`--audit-lock` 脚本侧行为零改动
- **接入 Trellis 工程框架**：`.trellis/`（规范 / 任务 / 会话记忆）与 Claude Code
  平台层（`.claude/` 的 trellis-* skills 与 hooks）、根目录 `AGENTS.md`。规范按本仓库
  实际结构回填为四个目录（engine / workflows / maintenance / guides），全部条目
  来自真实代码与真实踩坑并给出出处；init 预置的 frontend / backend 模板层
  不适用已删除。维护循环流程规范同步沉淀进 `spec/maintenance/`，
  maintain-loop skill 补充「与 Trellis 的关系」一节。对使用者无感知，
  对贡献者的可见变化是：`.trellis/spec/` 是动手前应读的规范入口

## [1.8.0] - 2026-09-13

本轮回到数据本身：`--write-lock` 从「能复现」补全为「可校验」（`--audit-lock` 抓住上游悄悄覆盖 tag 的现场）；三种只读检查支持 `--report-dir`，跑完留下与同步同款的报告；macOS 本地同步不再静默失去超时保护。

### 新增

- **检查报告落盘**：`--audit` / `--check-updates` / `--audit-lock` 支持
  `--report-dir`，产出与同步报告同款的 `.md` + `.json`。此前两个只读检查
  跑完什么都不留——结果只存在于日志与 Step Summary 里，想让结果留档或给
  别的系统消费只能截图。JSON 顶层带 `generated_at` 与汇总计数，逐条记录
  带状态与备注；md 与 Step Summary 三方同源，不会各自漂移。
  `Check-Registry` 体检工作流同步上传报告 Artifact（30 天保留，与同步
  报告同一机制——这不是引入新的存储，Artifact 本来就是项目的既有约定）

- **锁文件时效性校验**（`--audit-lock <文件>`）：只读校验 `--write-lock` 生成的
  锁文件——锁文件里每个「镜像@digest」的上游，现在还是不是锁定的那份。
  `--write-lock` 只完成了「能复现」这半件事：上游重新构建并覆盖同名 tag 时
  锁文件不会有任何变化，而下一次增量同步会把新内容静默搬过去，等集群行为
  异常才发现。三种状态：一致 / 漂移 / 无法判定；上游 tag 已删除算漂移
  （明确发生的变更，不是「查不到」）。比对口径与 `--write-lock` 同源，
  「一致」意味着的正是「当时锁的就是这份」。未锁定 digest 的行与锁文件标注行
  出现在报告里但不参与判定。不需要目标地址

### 修复

- **macOS 本地同步不再静默失去超时保护**：`setup_timeout` 找不到 `timeout` /
  `gtimeout` 时（macOS 默认就没有），现在用系统自带的 perl `alarm` 兜底。
  此前所有 macOS 本地使用者开箱即处于无保护状态——一个大镜像或网络挂起会
  卡住整个批量任务，而这正是 `--timeout` 要防的事；警告里建议的 coreutils
  则要求使用者为项目装系统依赖。CI 的 ubuntu 永远走不到这个分支，
  因此冒烟测试直接测兜底机制本身

---

## [1.7.0] - 2026-09-12

本轮把 v1.6.0 做出的两条只读检查从「本地 CLI 的能力」变成「谁都能用的能力」：新增体检工作流让不装 CLI 的人也能在网页上跑检查，检查结果也能推送通知，并把八条设计记录补进架构文档。三项都建立在「检查是只读的」之上，没有触碰「同步必须显式触发」这条红线。

### 新增

- **体检工作流**（`Check-Registry`）：把 v1.6.0 的两条只读检查带到 Actions 页面。
  此前它们只能在本地命令行跑，而项目的主力使用者是通过网页操作的人——不装 CLI、
  也不克隆仓库，于是「检查」这类最该被用上的能力反而只有少数人能用得上。用一个
  `mode` 输入选 `audit`（清单 vs 目标仓库）或 `updates`（清单 vs 上游）；结果进
  Step Summary，配了 webhook 就推送到群里（默认只在有落后 / 缺失 / 无法判定时打扰）。
  体检发现差异时运行**标红**：绿 = 一切正常，红 = 需要看看——不是「检查失败」，
  而是「检查有结论」。`updates` 模式不需要目标地址，工作流也不会把它传给脚本。
  **工作流只有手动触发**：需要定时的使用者可以 fork 后自行加一行 `schedule`
- **检查结果也能推送通知**：`--audit` 与 `--check-updates` 支持 `--notify-webhook`，
  补上「无人值守体检」的最后一块——此前只有同步结果能通知，定期跑体检仍然得有人
  天天去运行页面看。`--notify-on failure` 在检查模式下的含义是「有需要关注的项」
  （落后 / 缺失 / 无法判定，或有仓库没查成），全绿时不打扰；通知只列需要处理的
  条目（最多 20 条），不会把整张表推过去把重点淹没。`--notify-after-failures`
  仅同步模式适用，检查模式下显式传入会告警

### 变更

- **架构文档补齐 v1.6.0 引入的只读检查设计记录**：新增八条「为什么」——检查与搬运
  为什么拆开、「无法判定」为什么必须单独成类（含判据为何刻意保守）、`--audit` 与
  `--strip-attestation` 为什么互斥且直接报错、上游版本检查为什么不做语义化判断
  （附两个被否掉的方案）、两条检查为什么互斥、退出码 `2` 为什么包含「没查完」、
  命名规则为什么挂在每个目标上，以及为什么不给体检加定时触发器。顺带修正「代码结构」
  里两处滞后（`scorecard.yml` 与 `history.sh` 都没列进去）

### 修复

- **配了 webhook 时，macOS 上的同步会以非零退出**：`for line in "${alert_lines[@]}"`
  在数组为空时，bash 3.2（macOS 自带）配合 `set -u` 会抛 unbound variable——
  同步明明成功了，脚本却报错退出，使用者会以为同步失败。触发条件是「全部成功 +
  配置了 webhook + 在 bash 3.2 上运行」，CI 用的 bash 5 不报此错，因此一直没暴露。
  改为先判长度再遍历（与 `collect_images` 里的既有做法一致）

## [1.6.0] - 2026-09-12

本轮把「该同步什么」从同步动作里拆了出来：`--audit` 只读回答「仓库与清单的差距」，`--check-updates` 只读回答「上游有没有新版本」；`--dest-keep-path` 解开了多目标共用一个命名规则的别扭。此外补齐供应链加固（pin SHA / zizmor / Scorecard）、英文版 README 与 SUPPORT.md，并把维护流程沉淀为两个项目级 skill。

### 新增

- **按目标选择命名规则**（`--dest-keep-path`）：`--dest` 一直用压平规则（`/` → `_`），
  因为阿里云 ACR 个人版不支持多级仓库路径；而自建 Harbor 支持多级路径，保留原路径
  也更符合直觉。此前多目标只能共用一套规则，「阿里云给国内集群 + Harbor 做内部归档」
  这个最典型的组合必然有一边别扭。现在每个目标可以各自选择，两者可任意混用、顺序无关。
  注意：源 registry 带端口时（`localhost:5000/foo`）端口那段仍会压成下划线——
  仓库路径不允许出现冒号
- **上游版本检查**（`--check-updates`）：拉取上游 tag 列表与清单对比，报告哪些
  版本还没收录——上游发新版时没有任何机制会通知你，这是定期同步流程里唯一还需要
  人肉盯着的环节。**只报告，不修改清单**（升到哪个版本涉及兼容性判断，是人的决定）；
  **不做语义化版本判断、不过滤预发布**（上游命名未必规整，`latest` / `1.27-alpine`
  / `v1.32.0-rc.1` 混在一起时语义化比较会给出错误结论），只用版本序粗排并原样展示。
  默认只列版本序最大的 5 条（`--updates-limit` 可调），但总数始终给出。不需要
  目标地址，同一源仓库的多个 tag 只查一次上游
- **清单审计**（`--audit`）：只读检查清单里每个镜像在目标仓库中的状态，**不推送
  任何东西**，回答「我的仓库跟上清单了吗」——这个问题此前只有真的跑一次同步才能
  回答，而同步是会真推送的。四种状态分开报告：最新 / 落后 / 缺失 / 无法判定。
  「无法判定」单独占一类是刻意的：把网络抖动显示成「落后」，会让人去排查一个
  并不存在的问题；源自身取不到时报的也是「无法判定」，而不是目标「缺失」。
  与 `--strip-attestation` 互斥（后者会重建索引，审计只会给出一排假的「落后」）。
  退出码 `2` 表示「未得出全部最新」，含「没查完」的情况，便于接进 CI 做定期体检
- **英文版 README**（`README.en.md`）：面向国际使用者的完整入口，覆盖全部特性、
  快速开始、参数速查与常见场景；两个 README 顶部互相链接切换。完整文档仍以
  中文为主，英文版已注明。顺带修正中文 README 滞后的路线图——私有源凭证、
  历史趋势、完整性校验、多目标单拉取、供应链加固等已完成项此前仍不在列
- **skill 沉淀**：`maintain-loop` 新增「退出码与进程模型」硬规则（包装函数
  吞退出码、命令替换的子 shell 传值、find 退出码语义等，全部来自真实踩坑）
  与「修改 YAML 工作流的工具选择」；`oss-bootstrap` 新增供应链加固清单
  （pin SHA / persist-credentials / zizmor / Scorecard）、SUPPORT.md 与
  多语言 README 的条目
- **SUPPORT.md**：获取帮助入口（文档 → Discussions Q&A → Bug → 安全报告的
  分流路径），这是 GitHub 社区标准文件之一，此前只有 README 里的零散指引

### 安全

- **供应链加固**（对标 OSSF Scorecard 检查项）：所有工作流引用的 Actions 从
  版本号 pin 到 commit SHA（注释保留版本，Dependabot 仍会提更新 PR）；
  所有 checkout 加 `persist-credentials: false`，GITHUB_TOKEN 不再残留在
  runner 上；Dependabot 引入 7 天冷却期
- **工作流安全扫描**：CI 新增 zizmor job（容器按版本 pin），专查 Actions
  的已知反模式；豁免项集中在 `.github/zizmor.yml`，每条写明安全依据
- **OSSF Scorecard**：新增评分工作流（结果发布到公开评分页并上传 code
  scanning），README 加 Scorecard 徽章——供应链安全从「自己觉得做得好」
  变成「有公开体检报告」
- 教学文件 learn-github-actions.yaml 补上最小权限声明

---

## [1.5.0] - 2026-09-11

效率与多源场景：多目标不再重复拉取源镜像，凭证支持按仓库映射，架构文档补齐三轮迭代的设计记录。

### 新增

- **多目标同步源只拉取一次**：多个目标都需要推送时，源镜像先拉到本地 OCI 目录
  中转，再逐目标推送。此前每个目标各拉一次，大镜像（几 GB 的 ML 框架）会被
  成倍拉取时间与上游限流风险拖累。中转是**纯优化**：磁盘空间预检不足、拉取失败
  都自动退化为逐目标拉取并告警，不改变任何结果记录的语义；单目标与
  `--strip-attestation` 模式不走中转。拉取耗时计入第一个目标并在备注注明，
  避免耗时排行看起来不合理
- **按源仓库映射凭证**：新增 `--src-credentials <文件>`（或环境变量
  `SYNC_SRC_CREDENTIALS`，值为文件内容），每行「host 用户名 密码」。清单混有
  多个私有源（公司 Harbor + 私有 GHCR）时不必再拆分运行；未匹配到凭证的 host
  走匿名，公开镜像不受影响。与单一凭证（`--src-username`）互斥，混用直接报错；
  格式错误的行指明行号并中止——**错误信息不回显行内容**，因为行里有密码

### 文档

- [架构文档](docs/ARCHITECTURE.md)补齐 v1.3.0 以来的机制设计记录：镜像筛选的
  「只标记不删除」、源凭证不进命令行的威胁模型、完整性校验复用跳过判定的
  原因、通知阈值选「下载历史」而非「保存状态」的权衡、history.sh 的
  Artifact 即数据源、多目标中转的「纯优化」边界。每个取舍都写明被否掉的
  方案，供新场景下复用判断。同步流程图与「关键设计决策」同步更新
## [1.4.0] - 2026-09-11

同步质量：从「成功了吗」推进到「同步得对不对、快不快、出问题时是否值得被通知」。

### 新增

- **完整性校验**：新增 `--verify`，同步成功后逐平台比对源与目标的子 manifest
  digest——`skopeo copy` 返回 0 只说明命令跑完了，不代表每个平台都完整推上去了。
  有差异时明确列出缺失的平台并判定该镜像失败；拿不到 digest 时只告警不判失败。
  默认关闭（每个镜像多两次 inspect，大清单会变慢），三个工作流均开放了对应输入
- **耗时排行**：报告与 Step Summary 末尾新增「最慢的同步记录」——一批镜像的总耗时
  几乎总是被其中一两个主导，逐个翻表格看不出来。`history.sh --slowest` 则跨运行
  汇总平均耗时，同时给出波动范围：单次超时会把平均值拉高，看范围就能分辨
  「一直慢」还是「偶尔慢」，前者才值得优化
- **告警阈值**：新增 `--notify-after-failures`，同一个镜像连续失败 N 次才通知
  （默认 1，与历史行为一致）。每次失败都响的通知很快就没有人看了——上游抖动
  占了失败原因的一大部分，重跑就好。中间成功一次即清零，单个镜像独立计数。
  连续次数从历史运行的报告 Artifact 中推算，不引入任何新的存储；拿不到历史时
  按「连续失败 1 次」处理，宁可不通知也不基于猜测误报

---

## [1.3.0] - 2026-09-11

同步私有源、按需筛选，以及一个把历次报告汇成趋势的脚本。

### 新增

- **镜像筛选**：新增 `--filter` 与 `--exclude`，用正则从清单中挑选本次要同步的镜像
  （如 `--filter 'kube-' --exclude 'apiserver'`）。清单文件因此可以保持为「期望状态
  的完整记录」，不必为了临时筛选而改动它。被排除的镜像仍会出现在结果表中并标注
  原因——**悄悄消失是最危险的**，使用者会误以为它已经同步
- **私有源凭证**：新增 `--src-username` / `--src-password`（也可用环境变量
  `SYNC_SRC_USERNAME` / `SYNC_SRC_PASSWORD`），用于同步公司内部 Harbor、私有
  GHCR 包这类需要认证的源。此前这类镜像只会在拉取阶段 401，然后被记成一次普通
  的「同步失败」，看不出是缺凭证。凭证只写进 600 权限的临时文件、经
  `--src-authfile` 交给 skopeo，**不经过命令行**——命令行参数对同机其他进程可见，
  也容易被调用方的日志语句原样打印。源与目标的凭证刻意分开配置：把目标仓库的
  凭证发往源仓库，等于把「往我仓库推送」的权限交给一个你并不信任的第三方
- **同步历史趋势**：新增 `scripts/history.sh`，把历次同步报告摊在一起看——
  「某个镜像是不是反复失败」「哪些镜像最不稳定」这类问题，单看一份报告回答不了。
  它**复用已有的报告 Artifact**，不引入任何新的存储，因此也不会给仓库留下
  持续增长的提交历史。历史中有失败记录时以退出码 2 结束，便于接进别的检查
- **`--retry-delay`**：新增该参数以固定两次重试之间的间隔。默认不指定时 skopeo
  按失败次数指数退避，这是多数场景的最优解；固定间隔是为窗口式限流的自建仓库准备的
- [排错手册](docs/TROUBLESHOOTING.md)新增两条：Docker Hub 限流（`toomanyrequests`）
  与自建 registry 的 bcrypt 密码文件问题——后者的特点是不报错，registry 启动正常
  却每次认证都失败，极易被误判成 Secret 配错了。`unauthorized` 条目补上「怎么区分
  源仓库还是目标仓库报的错」，两者处理方式完全不同，此前只写了目标仓库

### 修复

- 修复 regctl 路径的平台列表解析：`printf '%s'` 不输出结尾换行，而 `read` 读到
  没有换行收尾的最后一段时会返回非零并结束循环。后果是单平台直接失败（一段都读不到），
  多平台**静默丢掉最后一个**——表现为 arm64 没同步却毫无报错。由于
  `detect_platforms` 的输出同样没有结尾换行，**所有走 regctl 路径的同步都在丢平台**。
  同时把该路径的 `--dry-run` 输出改为复述真正会执行的参数，此前它独立拼接、与实际
  行为脱节，正是这个缺陷长期未被发现的原因
- `--retries` 在 `--strip-attestation` 模式下不再静默失效。该模式走 regctl 路径，
  而 regclient 有自己的重试策略，此前传入的 `--retries` 被完全忽略却毫无提示——
  使用者以为自己控制住了重试次数。现在会明确告警说明它不生效及原因
- `--dest-exact` 的单镜像约束改为按**筛选之后**的数量判断。此前传 5 个镜像再用
  `--filter` 收敛到 1 个也会被拒绝，等于筛选能力用不上

---

## [1.2.0] - 2026-09-11

多目标同步、对自建 registry 的支持，以及一个会真正推送镜像的 CI 集成测试。

### 新增

- **多目标同步**：`--dest` 可重复指定，一次运行把同一个镜像推送到多个目标仓库。
  每个目标独立判定成败与跳过，报告中每个「镜像 × 目标」是一条独立记录，
  因此能一眼看出是哪个目标出了问题
- **TLS 校验开关**：新增 `--tls-verify`，可对自建的 HTTP registry 关闭证书校验
- **CI 真实同步集成测试**：新增一个会**真正推送镜像**的 job，用本地 registry
  容器验证首次同步、增量跳过、内容变化后重新同步三条路径，不需要任何外部凭证。
  此前这些路径只有 dry-run 覆盖，而 v1.1.0 暴露的三个缺陷恰恰全部发生在这里
- **发布说明改为组装生成**：由 CHANGELOG 手写说明、GitHub 原生变更清单
  （PR 列表与对比链接）与可选 AI 摘要三段拼成，发布时能完整看清本次改了什么

### 修复

- 修复 registry 地址含端口时生成非法目标仓库名的问题。仓库名允许的字符集
  不含冒号，而压平规则原先只替换 `/`，会把 `localhost:5000/source/hello`
  变成 `localhost:5000_source_hello`。现在会先分离出 tag，再对名称部分
  同时替换 `/` 与 `:`——只有落在最后一个 `/` 之后的冒号才是 tag 分隔符
- 增量跳过的比对原先未沿用 `--tls-verify` 设置，导致对自建 registry
  静默失效——每次都判定为「需要同步」，跳过形同虚设

---

## [1.1.0] - 2026-09-11

可观测性与性能：同步结果通知、镜像 digest 记录，以及并发与增量同步。

### 新增

- **镜像 digest 记录**：同步报告中记录源与目标的 digest。digest 是不可变的，
  而 tag 可能被上游覆盖——不记录它，就丢失了「这次同步的到底是哪一份镜像」。
  据此可以审计、可以复现，也是排查跳过判定问题时最直接的证据
- **锁文件**：新增 `--write-lock`，把镜像与 digest 写成可直接回喂给 `--file`
  的格式，用于精确复现。即便上游重新构建了同名 tag，锁定的仍是当时那一份
- **结果通知**：新增 `--notify-webhook`，同步结束后把结果推送到钉钉 / 飞书 / Slack。
  同步多是无人值守的，失败往往要等集群拉不到镜像才发现，通知正是为了补上这个盲区。
  服务商类型按地址自动识别；通知失败只告警，不影响同步的退出码
- **并发同步**：新增 `--concurrency`，可同时同步多个镜像。批量同步是典型的
  I/O 等待型任务，串行时绝大部分时间都耗在等待上，实测 4 个镜像并发后
  总耗时从 4 秒降到 1 秒
- **超时控制**：新增 `--timeout`，为单个镜像设置超时（默认 600 秒），
  避免某个大镜像或上游抽风卡住整个批量任务。未安装 `timeout` 命令时
  自动降级为不限制，并给出安装提示
- **增量跳过**：新增 `--skip-existing`，目标仓库已有完全相同的镜像时跳过推送。
  定期同步的场景下，绝大部分镜像都会被跳过，一次运行只需几秒
- 三个同步工作流均开放了上述能力的对应输入，默认开启增量跳过与并发
- CI 冒烟测试新增覆盖：并发执行、结果顺序、非法参数拒绝

### 修复

- 修复目标地址携带 digest 时生成非法推送目标的问题。源镜像形如
  `nginx:1.27@sha256:…` 时，原先会把 `@sha256:…` 一并拼进目标地址。
  使用锁文件时源天然带 digest，这个问题会立刻暴露
- 修复结果文件的字段分隔方式：制表符属于 bash 眼中的「IFS 空白字符」，
  相邻的两个会被合并成一个分隔符。当某个字段为空时其后的所有字段会整体
  左移（表现为目标 digest 被读成空值）。改用非空白的 Unit Separator

### 变更

- 增量跳过的比较依据由「顶层 manifest 的完整 JSON」改为「各平台子 manifest
  的 digest」。前者对 mediaType、annotations 与字段顺序都敏感，源与目标
  由不同 registry 提供时会误判为不同
- 比较时排除 Windows 平台：其 manifest 在传输过程中会被重新生成、digest
  必然改变，纳入比较会让跳过永远不成立
- 单个镜像的处理改为在独立子进程中执行，结果通过带序号的临时文件汇总，
  保证并发模式下结果表的顺序仍与输入顺序一致
- 校验 `--concurrency` / `--timeout` / `--retries` 的取值，非法参数在
  同步开始前就被拒绝，而不是跑到一半才出错

---

## [1.0.0] - 2026-09-11

首次以完整的开源项目形态发布。

从「几个能跑的工作流」整理为一个结构完整的项目：同步逻辑收敛为可复用的引擎、
补齐治理文件与文档体系、引入 CI 与仓库自动化，并修复了若干会导致静默失败的缺陷。

### 新增

- **批量同步**：`images_src` 现在支持一次传入多个镜像（换行、逗号或空格分隔），
  单个镜像失败不会中断其余镜像的同步，结束后输出汇总表
- **同步结果摘要**：每次运行生成 GitHub Step Summary 表格，包含每个镜像的目标地址、
  架构列表与耗时，无需翻日志即可确认结果
- **同步报告产物**：运行结束后可下载 Markdown / JSON 格式的同步报告（Artifact）
- **本地 CLI**：新增 `scripts/sync.sh`，可在本地复现同一套同步逻辑，支持 `--dry-run`
- **统一校验入口**：新增 `scripts/lint.sh`，一条命令跑完 CI 的全部静态检查
- **输入校验**：同步前校验镜像引用格式，格式错误立即标记失败并给出原因，
  不会浪费一次网络请求
- **可配置重试**：`scripts/sync.sh` 支持 `--retries` 指定失败重试次数（默认 3 次）
- 项目文档体系：`docs/ARCHITECTURE.md`（原理）、`docs/TROUBLESHOOTING.md`（排错）、
  `docs/MAINTAINER_GUIDE.md`（维护者手册）、`docs/BACKGROUND.md`（原始教程归档）
- 开源治理文件：`LICENSE`、`CONTRIBUTING.md`、`CODE_OF_CONDUCT.md`、`SECURITY.md`、
  `CODEOWNERS`、Issue / PR 模板
- CI 工作流：`actionlint` + `yamllint` + `shellcheck` + 提交信息规范校验
- 自动化：PR 自动打标签、首个贡献者欢迎、Issue / PR 过期提醒、自动发布

### 变更

- **目标仓库地址不再硬编码**：`registry.cn-shenzhen.aliyuncs.com/nicholyx` 收敛为
  `DEST_REGISTRY` 变量，可通过仓库变量 `ALIYUNCS_REGISTRY` 覆盖
- **登录方式改为 `docker login`**：使登录地址与推送地址共用同一个变量，避免改一处漏一处
- **regctl 版本固定为 `v0.11.6`**：原先使用 `releases/latest` 下载，导致同一工作流
  在不同时间可能拿到不同版本，无法复现
- **工作流运行名称**现在包含源镜像名，便于在 Actions 列表中区分每次同步
- 移除了两个与镜像同步无关的多余步骤（`actions/checkout` 与 `docker/setup-buildx-action`）
- `platforms` 改为可通过工作流输入配置，不再写死

### 修复

- **Harbor 工作流丢失多架构**：补上 `skopeo copy --all`。此前同步 multi-arch 镜像时
  只搬运 runner 所在的 amd64 平台，arm64 用户拉取会报 `no matching manifest`
- **regctl 分支在特定输入下生成非法镜像名**：源镜像带 `docker://` 前缀时，
  目标仓库名会连同前缀一起参与替换，生成 `docker:__xxx` 这类非法引用
- **注释与实现不符**：注释写「替换 `/` 为 `__`」，实际早已改为单个 `_`，会误导后续维护者
- 修正了工作流中误导性的步骤名（登录阿里云 / Harbor 的步骤原名为 `Login to Docker Hub`）

### 安全

- 所有来自 `workflow_dispatch` 的输入不再直接插值到 `run:` 脚本，统一经 `env:` 中转，
  消除表达式注入风险
- 新增 `SECURITY.md` 说明本项目的威胁模型与漏洞报告流程
- 忽略 `.qoder/` 等编辑器本地配置，避免误提交

---

## 历史版本

以下版本早于本日志的建立，依据 Git 提交历史回溯整理。

### [0.3.0] - 2026-08-29

#### 修复

- 用 `regctl index create` 重建不含 attestation 的镜像索引，
  规避阿里云 ACR 对 OCI 1.1 空 blob 的 `unknown manifest class` 拒绝
- `regctl --platforms` 需重复传参而非逗号分隔
- 改用 GitHub Releases 下载 `regctl`（原先的安装方式已失效）

### [0.2.0] - 2026-08-29

#### 新增

- 支持同步 multi-arch 镜像（amd64 / arm64）
- 增加剔除 attestation manifest 的同步选项

#### 修复

- 修正目标仓库名中斜杠替换导致无法上传到阿里云的问题

### [0.1.1] - 2025

#### 新增

- 新增 Harbor 仓库同步工作流

### [0.1.0] - 2024-06-26

#### 新增

- 首个版本：基于 `skopeo` 将国外镜像同步至阿里云容器镜像服务

---

## 版本说明

- **Unreleased** 段落由贡献者在 PR 中随手补充，维护者在发布时归入正式版本号
- 每个版本的分类固定为：`新增` / `变更` / `弃用` / `移除` / `修复` / `安全`
- 破坏性变更会在标题后标注 **BREAKING**

[Unreleased]: https://github.com/nicholyx/action-sync-images/commits/main
